# Merge into your ESPHome secrets.yaml (do not commit the real one).
wifi_ssid: "YourWiFi"
wifi_password: "YourWiFiPassword"

# Same broker + credentials the Pi uses. Use HA's LAN IP, not the public host.
mqtt_broker: "192.168.1.x"
mqtt_username: "basil-capture"
mqtt_password: "CHANGE_ME"
